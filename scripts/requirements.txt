# CrimeMap SA data pipeline dependencies.
#
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r scripts/requirements.txt

pandas>=2.0
requests>=2.31
python-dotenv>=1.0

# Needed only when the source file is distributed as .xlsx.
openpyxl>=3.1
